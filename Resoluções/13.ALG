algoritmo "treze"
var
   sal, nSal : real
inicio
      escreva("Digite o salário do funcionário: R$")
      leia(sal)
      nSal <- sal * 1.15
      escreva("O novo salário do funcionario passou de R$", sal, " para R$", nSal:8:2)
fimalgoritmo